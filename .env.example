# TillDash Mail Server Configuration

# Domain Configuration
MAIL_DOMAIN=tilldash.com
MAIL_HOSTNAME=mail.tilldash.com

# Email Accounts (will be created automatically)
# Format: email|password
MAIL_ACCOUNTS=support@tilldash.com|change_this_password,noreply@tilldash.com|change_this_password

# SSL/TLS Configuration
# Set to 'letsencrypt' for automatic SSL, or 'manual' for custom certificates
SSL_TYPE=manual

# Let's Encrypt (if using automatic SSL)
LETSENCRYPT_EMAIL=admin@tilldash.com
LETSENCRYPT_HOST=mail.tilldash.com

# Security Settings
ENABLE_FAIL2BAN=1
ENABLE_SPAMASSASSIN=1
ENABLE_CLAMAV=0  # Disable for resource efficiency

# Rate Limiting (emails per hour per sender)
POSTFIX_MAILBOX_SIZE_LIMIT=0
POSTFIX_MESSAGE_SIZE_LIMIT=51200000  # 50MB

# Server IP (for DNS setup)
SERVER_PUBLIC_IP=your.server.ip.here
